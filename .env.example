# Optional: Claude for goal parsing. A deterministic fallback is used if unset.
ANTHROPIC_API_KEY=

# Optional: Razorpay test-mode. A mock client is used if unset.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Optional override for tests
AUDIT_DB_PATH=
ANTHROPIC_MODEL=claude-sonnet-4-5
